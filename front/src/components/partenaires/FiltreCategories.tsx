import type { Categorie } from "@/types/domaine";

/**
 * Les categories proviennent des donnees, jamais d'une liste ecrite
 * dans ce fichier. Ajouter, renommer ou retirer une categorie ne doit
 * toucher aucune ligne d'interface (B. Sellami).
 */
export function FiltreCategories({ categories }: { categories: Categorie[] }) {
  throw new Error("Non implemente");
}
