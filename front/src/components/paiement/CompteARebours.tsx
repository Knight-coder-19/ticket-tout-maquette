"use client";

/**
 * Compte a rebours du code de paiement, avec bouton de regeneration.
 * Le salarie ne doit jamais se retrouver bloque en caisse.
 */
export function CompteARebours({ expireLe }: { expireLe: string }) {
  throw new Error("Non implemente");
}
