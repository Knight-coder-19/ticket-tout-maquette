/**
 * Ecran vide.
 * Une categorie sans partenaire s'affiche proprement au lieu de casser
 * la mise en page (B. Sellami). Un ecran vide est une invitation a agir,
 * pas un message d'excuse.
 */
export function EtatVide({ titre, action }: { titre: string; action?: React.ReactNode }) {
  throw new Error("Non implemente");
}
