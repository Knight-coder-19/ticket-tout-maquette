// Implement parse_and_stage (SHA-256 fingerprint, refuse a duplicate file, parse, resolve every matricule,
// return a preview listing the errors) then validate_batch producing one operation per row.
// A batch holding a single error is rejected as a whole. Priority: P2
