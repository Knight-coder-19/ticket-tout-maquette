// Implement create_session, validate_session(conn, token_hash, clock), revoke_session and revoke_all_for_user.
// Store only the token hash; revoke_all_for_user is called when an account is suspended.
// Priority: P0
