// Define GENESIS_HASH and entry_hash(seq, operation_id, account_id, direction, amount, recorded_at, prev_hash).
// Document the canonical serialization in a comment: any change to it invalidates the existing chain.
// Priority: P0
