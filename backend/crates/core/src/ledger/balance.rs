// Implement recompute_balance and recompute_held from the journal, and verify_chain(from_seq) returning
// the first inconsistent seq. Used by the invariant tests, the worker, and GET /admin/audit/verify.
// Priority: P0
