// Implement approve(tx, admin, partner_id) and reject(tx, admin, partner_id, reason): fill reviewed_by,
// reviewed_at and review_reason, and write to audit_log. approve must also check that no highlight exists
// for a partner that would become ineligible. Priority: P1
