// Define the partner DTOs: summary, transaction page, settle request carrying token_ref plus scanned_at,
// payment response, and the resync batch with a per-row status so a failing row never drops the batch.
// Priority: P0
