// Define the employee request and response DTOs: balance { settled, held, available }, transaction page,
// minister picks, authorize request, and the IssuedToken response (jti, short_code, amount, expires_at,
// qr_payload). Amounts stay plain integers, the session token never appears in a body. Priority: P0
