// Wire partner approve and reject, account suspension, the four highlight routes of section 8 (GET, POST,
// DELETE /{id}, PUT /reorder), topups, batches and their validation, compensations, GET /admin/dashboard
// and GET /admin/audit/verify. All behind AuthUser<Admin> and the audit layer. Priority: P1
